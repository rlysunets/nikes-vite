import React from 'react'

const SectionTitle = ({ title }) => {
   return (
      <h2 className="text-5xl lg:text-4xl sm:text-3xl font-bold text-slate-900 drop-shadow-lg mb-10">{title}</h2>
   )
}

export default SectionTitle